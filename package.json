{
  "name": "model-council-ui",
  "version": "0.2.95",
  "description": "Web UI for model-council-mcp — run multi-model council deliberations from a chat interface",
  "type": "module",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"node server/index.mjs\" \"npm --workspace web run dev\"",
    "build": "npm --workspace web run build",
    "start": "NODE_ENV=production node server/index.mjs",
    "check": "npm --workspace web run check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "Apache-2.0"
}
